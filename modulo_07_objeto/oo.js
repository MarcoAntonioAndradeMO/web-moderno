// Código Não Executável

// Procedural => Baseado no procedimento/função
processamento(valor1, valor2, valor3)

// OO => Orientação a Objeto
// Estruturas que geram objetos
// Mais próximo ao mundo real

objeto = {
    valor1,
    valor2,
    valor3,
    processamento() {
        //...
    }
}

objeto.processamento(); // Foco passou a ser o objeto

// Abstração => Simplificar o objeto real para dentro do seu sistema de forma a mapear apenas o que é relevante.
// As abstrações de um objeto, podem ser diferentes ao variar com o sistema.

// Princípios Importantes
// 1. Abstração
// 2. Encapsulamento
// 3. Herança (prototype) => Um elemento pai passa características para os elementos filhos.
// 4. Polimorfismo => Um conceito genérico que pode ser apontado para diferentes objetos. (multiplas formas).